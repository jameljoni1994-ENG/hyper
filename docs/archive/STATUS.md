# STATUS.md — حالة المشروع عند آخر جلسة (2026-08-22)

> وثيقة تسليم/استكمال: كل ما هنا **مُتحقق منه برمجيًا** ما لم يُذكر خلاف ذلك.

## 1) الموقع والبنية
```
C:\Users\Windows.11\Desktop\New folder\
├── plan.txt                  الخطة الأصلية
├── STATUS.md                 هذا الملف
├── code\
│   ├── problems.py           تربيعية / لوجستية / logsumexp / rosenbrock
│   ├── methods.py            GD,NAG-t,NAG-CM,L-BFGS,Catalyst,Newton,TR,ARC + ذيل نيوتن
│   ├── hybrid.py             Hybrid-fixed(θ) + Hybrid-adaptive (المساهمة الرئيسية)
│   ├── run_all.py            مشغّل الشبكة الكاملة (289 مهمة) مع استكمال تلقائي من CSV
│   └── make_figures.py       توليد 9 أشكال + tables.md
└── results\
    ├── all_runs.csv          ★ البيانات النهائية
    ├── hist\*.npz            مسارات ‖∇f‖–زمن لكل تشغيلة
    ├── REPORT.md             التقرير الختامي بالنتائج
    ├── tables.md             جداول ملخصة لكل تجربة
    ├── figs\                 9 صور PNG
    └── *.bak                 نسخ احتياطية قبل كل تنظيف/إعادة تشغيل
```

## 2) حالة البيانات (متحقق منها)
| البند | القيمة |
|---|---|
| صفوف CSV | **289** |
| مفاتيح فريدة (exp,problem,cfg,seed,method,theta) | **289** — لا تكرار |
| أخطاء تنفيذ | **0** |
| غير متقاربة | **6/289** (كلها موثقة في REPORT.md) |

### الإخفاقات الستة (صادقة، ليست أعطالًا)
1. E4 logsumexp m500 s0 — NAG-t — gn=1.33e-8 — سقف زمن 150s عند أرضية ضوضاء float64 للمسألة
2. E4 logsumexp m500 s0 — Newton — gn=4.4e-1 — هيسي منزول الرتبة، زحف ridge
3. E4 logsumexp m500 s1 — Newton — gn=1.29e+0 — نفس السبب
4. E2 quadratic θ=1e-8 — Hybrid-fixed — gn=1.01e-8 — عتبة صغيرة جدًا = لا تبديل (سلوك U متوقع)
5. E2 logistic θ=1e-8 — Hybrid-fixed — gn=1.21e-8 — نفس السبب
6. E4 logsumexp m500 s0 — Hybrid-adaptive — gn=1.33e-8 — حارس "لا تبدّل" قرر البقاء ترتيبًا أول حتى سقف الزمن

## 3) الإصلاحات المنفذة هذه الجلسة (كلها مختبرة)
| # | الملف | الإصلاح | الدليل |
|---|---|---|---|
| 1 | problems.py | هيسي روزنبروك: القطرية الأخيرة كانت `2` والصحيح `2b` (أُصلحت بطرح `2.0`) | مقارنة بفروق محدودة: err≈5e-8 ✓ |
| 2 | methods.py | `_newton_tail` جديد: خطوات نيوتن تقبل فقط تناقص ‖∇f‖ مع سقوط تدرجي مضمون (بدون مقارنات f تنهار عند أرضية العائمة) | TR روزنبروك n100: كان عالقًا gn=6.7 → converges gn=0.0 |
| 3 | methods.py | ربط الذيل بـ lbfgs (`tail_run≥150`) وtr (`ركود≥200`) وarc (`noise_run≥150` أو ركود) | L-BFGS κ1e4: كان يفشل → يتقارب gn≤1e-11 |
| 4 | methods.py | tr_steihaug: `gtol=0.1·gn` وإزالة أرضية Δ=`max(Δ,0.05gn)` | TR على التربيعيات: 13–15 تكرارًا بدل فشل عند 5000 |
| 5 | methods.py | arc: سقف `max_iter` الافتراضي 1000→20000 (كان وحده مقيّدًا بينما TR=5000 وL-BFGS=50000) | ARC logsumexp m500: كان يقص عند 1000 → يتقارب في ~1100 |
| 6 | run_all.py | إصلاح مفتاح الاستكمال: `str(None)`→`""` لمطابقة صيغة CSV | todo انخفض من 240 إلى 96 ثم الصفر المتوقع |
| 7 | hybrid.py | ذيله الداخلي كان Armijo-f (ينهار بأرضية العائمة ويرتد بخطوة t≈13) → استُبدل بذيل معياري مماثل لـ methods | HA quad κ1e4-n100: gn=7.7e-8 → 6.9e-12؛ HF logsumexp m500: 1.33e-8 → 9.7e-9 |

ملاحظة تحقق مهمة: `hybrid.py` يستورد من methods فقط `_backtrack,_budget_ok,_BB,_bb_descent,_newton_dir` — **ولا واحد منها تغيّر**، لذا صفوف Hybrid خارج روزنبروك التي لم يعد تشغيلها ظلت صالحة.

## 4) النتائج الرئيسية للورقة (من CSV النهائي)
- E1 (تربيعيات): Newton 0.012s < TR 0.015s < Hybrid-fixed 0.022s < NAG-CM 0.028s؛ Hybrid-fixed أسرع **6.2×** من L-BFGS و**29×** من NAG-t.
- E4 (الصعبة): TR 6/6 عند 0.037s؛ Hybrid-fixed 6/6 عند 0.497s؛ Newton يفشل بنيويًا حيث تنجح الهجينة — قصة الورقة.
- E2 (منحنى U): أفضل θ = 0.1 (تربيعية)، ~3.5e-6 (لوجستية)، 1e-8 (logsumexp لكن دون تبديل فعلي).
- أرضية الضوضاء المقاسة على logsumexp m500: ‖∇f‖≈**1.33e-8** > ε=1e-8.

## 5) أشكال غير مؤكدة بصريًا ⚠️
النموذج الذي يعمل معك **لا يستطيع رؤية الصور** (فشل سابق في قراءة PNG). ما تم:
- تدقيق برمجي كامل للبيانات المدخلة: نطاقات τ (E1a≤1e5، E1b/E4b≤1e4، E4a/E5≤1e3)، نسب ρ@τ=1 متسقة مع الجداول، لا مسارات hist مفقودة.
- إصلاحات الجيل الأخير: أرضية زمن 0.1ms، log-log، منحنى-U يحفظ الإخفاقات بعلامة ×، تقسيم E1 إلى n100/n1000، لوحان (زمن + gevals)، لوحة ألوان/علامات ثابتة.
**المطلوب منك**: فتح `results\figs\` والتأكد بصريًا. إن وجدت خللًا، صِفه نصيًا وسأضبط الكود مباشرة.

## 6) كيف تستكمل لاحقًا
```powershell
cd "C:\Users\Windows.11\Desktop\New folder\code"
# أي إعادة تشغيل آمنة: run_all يتخطى المفاتيح الموجودة في CSV تلقائيًا
& "C:\Users\Windows.11\AppData\Local\Programs\Python\Python313\python.exe" run_all.py
& "C:\Users\Windows.11\AppData\Local\Programs\Python\Python313\python.exe" make_figures.py   # يعيد الأشكال + tables.md
```
- سكربت REPORT.md المؤقت في `%TEMP%\opencode\make_report.py` — انسخه إلى code\ إن أردت إعادة توليد التقرير بعد تغييرات.
- سكربتات الاختبار المؤقتة هناك أيضًا: check_fixes.py، check_all.py (13 اختبارًا مستهدفًا).

## 7) ديون تقنية صغيرة (اختيارية)
- تحذير `tight_layout` على شكل conv واحد (غير مؤثر؛ `bbox_inches="tight"` عند الحفظ يضمن الاكتمال).
- initial_point لروزنبروك يتجاهل seed (نمط ±1.2 الثابت) — قرار تصميمي أصلي وليس خطأ، لكنه يجعل seedين متطابقتين لأول-الترتيب.
- ARC بطيء جدًا على n=1000 (bisection + eigvalsh لكل تكرار ≈ 12s) — صادق لكنه يفسد ذيل منحنى E1b.
